% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confidence_intervals.R
\name{confidence_intervals}
\alias{confidence_intervals}
\title{Summarise a bootstrap distribution into confidence intervals}
\usage{
confidence_intervals(
  boot_strap,
  probs = c(lower95 = 0.025, lower80 = 0.1, median = 0.5, upper80 = 0.9, upper95 = 0.975),
  method = c("percentile", "basic", "bca"),
  min_n = 30L
)
}
\arguments{
\item{boot_strap}{A \code{boot_strap} object from \code{\link[=bootstrap_measures]{bootstrap_measures()}}.}

\item{probs}{Named quantile probabilities; each becomes an interval column.}

\item{method}{\code{"percentile"} (quantiles of the draws), \code{"basic"} (those quantiles
reflected about the observed estimate), or \code{"bca"} (bias-corrected and accelerated,
using the per-customer jackknife -- a two-sample jackknife for comparisons).}

\item{min_n}{Cells with fewer than \code{min_n} customers are flagged \code{reliable = FALSE}
and trigger a warning.}
}
\value{
A data frame with the grouping columns, \code{kpi}, \code{estimate}, one column per
entry of \code{probs}, \code{n}, \code{reliable}, and \code{nonfinite} (the fraction of draws that were
non-finite and so excluded from the quantiles).
}
\description{
Turns a \code{\link[=bootstrap_measures]{bootstrap_measures()}} result into a tidy table: one row per cell (or
comparison) and KPI, with the observed point estimate and the requested quantiles.
Per-arm intervals come from a plain result; lift / difference intervals come from a
compared one -- "direct" versus "relative" is which object you pass.
}
\details{
Bootstrap intervals get unreliable when a cell is small (the resample space is
coarse), so each row carries its \code{n} and a \code{reliable} flag rather than being silently
dropped, and the call warns when any cell falls below \code{min_n}.
}
\examples{
customers <- dplyr::group_by(
  data.frame(spend = rgamma(100, 2), variant = "A"), variant
)
bs <- bootstrap_measures(customers, arpu = mean(spend), iterations = 1000, seed = 1)
confidence_intervals(bs)
}
\seealso{
\code{\link[=bootstrap_measures]{bootstrap_measures()}}
}
